"""
Volume Demand ML — Variant-level volume forecasting.

Forecasts how many mg (or units) of each variant will be sold over 14 days.
Uses the same two-stage algorithm as item demand: P(sold) × predicted_volume.
"""
