# Contract 1.9 fixtures

Golden request/response payloads for 100% coverage of the `desktop_analytics_app_sync` wire contract.

> **Strict-only (2026-07-09):** the legacy batched ingest endpoints (`POST customer-merges/ingest`, `POST menu-merges/ingest`, `POST menu-mapping-verifications/ingest`) were **removed** and now return `404`. Fixtures for those endpoints are retained as **historical** references to the wire shape the commit path still persists; mutations land through the commit endpoints (§19, §21).

> **Forecasts server-authored only:** `POST forecasts/ingest` (§18.4) is **removed** and returns `404`; its request/response fixtures are gone because no code path persists that shape any more. `GET forecasts/bootstrap` (§18.5) is **also removed in 1.2** and returns `404` — it was a route alias kept because the deployed desktop built that path. `GET forecasts/central-bootstrap` is the only name, returning the §22.6 `runs` / `rows` / `weather_rows` body; the legacy six-array response is gone and `legacy=1` is refused with `400` (`code: retired_parameter`) rather than ignored.

| File | Endpoint(s) | Purpose |
|------|-------------|---------|
| `customer_merge_sync_fixtures.json` | ~~`POST customer-merges/ingest`~~ (removed), `GET customer-merges` | Historical customer merge ingest request/response; delta pull (applied + undo) still authoritative. |
| `customer_merge_event_fixtures.json` | — (shared extraction parity) | Shared event shapes pinning extraction parity between desktop client and server. |
| `customer_mutations_fixtures.json` | `POST customer-mutations/commit`, `GET customer-mutations/{id}` | Strict-mode customer mutation commit (applied + undo), conflict, status reconcile. |
| `menu_merge_sync_fixtures.json` | ~~`POST menu-merges/ingest`~~ (removed), `GET menu-merges` | Historical menu merge ingest request/response; delta pull (applied + undo + variant merge) still authoritative. |
| `menu_merge_event_fixtures.json` | — (shared extraction parity) | Shared event shapes pinning assignment extraction parity between desktop client and server. |
| `menu_mutations_fixtures.json` | `POST menu-mutations/commit`, `GET menu-mutations/{id}` | Strict-mode menu mutation commit (assignment mutations, verify, catalog_update, derived_assignment.sync), conflict, status reconcile, the retired-assignment-field refusal (§24), and the refusal of a canonical write from a restaurant that belongs to a menu group (§25.3). |
| `menu_mapping_verification_sync_fixtures.json` | ~~`POST menu-mapping-verifications/ingest`~~ (removed), `GET menu-mapping-verifications` | Historical mapping verification ingest; pull (verified, reopened, bulk_verified) still authoritative. |
| `menu_mapping_verification_event_fixtures.json` | — (shared extraction parity) | Shared event shapes pinning `is_verified` extraction parity between desktop client and server. |
| `menu_bootstrap_fixtures.json` | `POST menu-bootstrap/ingest`, `GET menu-bootstrap/latest` | Bootstrap snapshot ingest/pull (seed_only, empty scope). The `shared_pos_catalog` and `group_pos_alias_observation` channels are retired (§25.12). |
| `menu_assignments_snapshot_fixtures.json` | `GET menu-assignments/snapshot` | Materialized assignment snapshot with dual watermarks (data + empty page). |
| `menu_catalog_snapshot_fixtures.json` | `GET menu-catalog-snapshot/latest` | Normalized catalog checkpoint (data + empty scope). |
| `telemetry_fixtures.json` | `POST errors/ingest`, `POST learning/ingest`, `POST conversations/sync` | Analytics telemetry ingest (errors, AI logs, feedback, conversations). |
| `forecast_sync_fixtures.json` | ~~`POST forecasts/ingest`~~ (removed), ~~`GET forecasts/bootstrap`~~ (removed), `GET forecasts/status`, `GET forecasts/central-bootstrap`, `GET forecasts/delta`, `GET forecasts/revenue`, `GET forecasts/items`, `GET forecasts/volume`, `GET forecasts/weather` (not yet implemented) | Server-authored central forecasting (status, bootstrap, delta, chart reads). Both the desktop-upload path and the `forecasts/bootstrap` alias are `404`; `forecasts/central-bootstrap` returns the §22.6 body. |
| `analytics_stream_fixtures.json` | `GET /analytics/orders`, `/analytics/order-items`, `/analytics/addons`, `/analytics/discounts`, `/analytics/restaurants`, plus two `/desktop-analytics-sync/` selector refusals | Raw analytics streams and restaurant selection (§23): pages for all four streams, the allowed-restaurants profile list, every selector/pagination error, the query-string-selector refusal and the retired-query-parameter refusal (§24). Mounted at `/analytics/` and authenticated with `X-API-Key`, except the two sync-route refusals, which use the Bearer token. |
| `global_menu_fixtures.json` | `GET global-menu/snapshot`, `GET global-menu/events`, `GET global-menu/status`, `GET global-menu/history`, `POST global-menu/mutations/preview`, `POST global-menu/mutations/commit` | Global menu groups (§25): the canonical catalog, restaurant-qualified POS rules, the event tail, capabilities and coverage status, unified legacy/global history, the merge and enrolment (`global_locator.map`) preview/commit pairs, and every refusal envelope including the not-provisioned deployment error. |
| `error_response_fixtures.json` | All endpoints | Common error/edge-case payloads (401, 400, 404, 409, 422, 500) and edge-case success responses (empty pulls, duplicates, rejected events). |

**Contract version:** `1.9` — matches `central_server_analytics_app_api_contract.md` revision `1.9`.

> **Breaking change in 1.1 (§23):** `X-Restaurant-ID` is required on every scoped route — no default restaurant, no `?restaurant_id=` alternative — and the child analytics streams no longer return `order_pk` / `order_item_pk`.
>
> **Breaking change in 1.2 (§24):** the `strict_mode_enabled` response key is gone from every payload; a client `order_line_key` / `petpooja_order_id` / `petpooja_itemid` on an assignment write is a `400 retired_field`; the `legacy` and `scope_key` query parameters are a `400 retired_parameter` on every scoped route; `forecasts/bootstrap` is a `404`; and the §17.6 assignment snapshot no longer filters out the collapsed `menu_item_id`s one desktop release could not map. Fixtures reflect 1.2 only — there is no 1.0 or 1.1 compatibility mode, and none is coming: the desktop is rebuilt against this revision.

> **Additive change in 1.3 (§25):** global menu groups. `GET /analytics/restaurants/` gains the always-present `menu_group_id` / `menu_capabilities` keys, the §17.6 assignment snapshot gains `global_menu_item_id` / `global_variant_id` when a group has linked the row, and `global_menu_fixtures.json` covers the new routes. Nothing is retired and a 1.2 client keeps working: a restaurant in no menu group behaves exactly as before.

> **Breaking global-menu freeze in 1.4 (§25):** the provisional unversioned snapshot/event payloads are replaced by schema-versioned envelopes using `after` / `next_cursor` / `has_more`, a coherent nested snapshot watermark, and complete semantic event deltas with tombstones. Assignment snapshots always carry schema/group metadata. There is one controlled client and capability remains off until it is rebuilt, so no provisional 1.3 global-wire adapter is retained.

> **Global-menu corrections in 1.5 and 1.6 (§25):** the implemented preview/undo/lifecycle/event-scope/backfill behavior is pinned by the global fixtures. No adapter for the earlier provisional global wire is retained.

> **Breaking global-menu removal in 1.9 (§9, §25.12):** revision 1.7's shared-Petpooja catalog policy and revision 1.8's group-owned POS alias policy are both **removed**, and so is the lifecycle ladder that predated them. `global_menu_alias_fixtures.json` is deleted; `global_menu_fixtures.json` no longer carries a rule `price`, a `status` rung or any `shared_pos_*` / `alias_*` status field, and gains the `global_locator.map` preview/commit pair that is now how a joining restaurant's rows are resolved, plus the `global_menu_group_not_provisioned` deployment refusal. `menu_bootstrap_fixtures.json` drops the observation channel and its acknowledgement key, `menu_mutations_fixtures.json` replaces `global_menu_shadow_write_blocked` with `global_menu_canonical_write_blocked`, and `analytics_stream_fixtures.json` advertises the four capabilities together. Neither policy was ever advertised on a production group, so nothing carries an adapter for either.

**Every §24 code has a fixture, and every fixture is pinned.** `retired_parameter`, `retired_field` and `restaurant_selector_not_a_parameter` each carry a golden body compared against a live response by the server suites (`analytics.tests.Phase4ContractFixtureTests`, `desktop_analytics_app_sync.tests.SyncRestaurantSelectorTests` and `.MenuMutationCommitPhase2Tests`). A published code the server never sends would be the same silent failure the refusals exist to prevent, one level up. `analytics_stream_fixtures.json` and `global_menu_fixtures.json` additionally fail their suites if any fixture in the file has no test pinning it. The §25 refusal and empty-page fixtures are compared byte-for-byte; the data-bearing ones are compared by field *shape*, because global ids are server-issued per install and a value comparison would pin nothing but the test's own random uuids.

**Enablement:** Sync endpoints share `Authorization: Bearer <SYNC_API_KEY>` auth (§4.1) under base path `/desktop-analytics-sync/`. The §23 analytics streams live at `/analytics/` and authenticate with `X-API-Key`. Both require `X-Restaurant-ID` on every scoped route.
